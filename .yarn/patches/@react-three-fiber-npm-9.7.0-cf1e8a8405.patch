diff --git a/dist/events-156d8d12.esm.js b/dist/events-156d8d12.esm.js
index 9681d0c62238d67f2d5ad389307dfd45ee8ba7ba..bd1d6000d04393364fc48cbe9e5b35de14456215 100644
--- a/dist/events-156d8d12.esm.js
+++ b/dist/events-156d8d12.esm.js
@@ -11,6 +11,49 @@ var threeTypes = /*#__PURE__*/Object.freeze({
   __proto__: null
 });
 
+// Three.js deprecated Clock in r183. Keep Fiber's clock contract while
+// delegating time measurement to the supported Timer implementation.
+class TimerClock {
+  constructor(autoStart = true) {
+    this.autoStart = autoStart;
+    this.startTime = 0;
+    this.oldTime = 0;
+    this.elapsedTime = 0;
+    this.running = false;
+    this.timer = new THREE.Timer();
+  }
+  start() {
+    this.timer.reset();
+    this.startTime = performance.now();
+    this.oldTime = this.startTime;
+    this.elapsedTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.getElapsedTime();
+    this.running = false;
+    this.autoStart = false;
+  }
+  getElapsedTime() {
+    this.getDelta();
+    return this.elapsedTime;
+  }
+  getDelta() {
+    let diff = 0;
+    if (this.autoStart && !this.running) {
+      this.start();
+      return 0;
+    }
+    if (this.running) {
+      this.timer.update();
+      diff = this.timer.getDelta();
+      this.oldTime = performance.now();
+      this.elapsedTime += diff;
+    }
+    return diff;
+  }
+}
+
 /**
  * Returns the instance's initial (outmost) root.
  */
@@ -1013,7 +1056,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: new TimerClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-4c71f21f.cjs.prod.js b/dist/events-4c71f21f.cjs.prod.js
index 65e702ef67d954387307b168529752690a586fd0..b7b1b5117ff41324a5b5a93ebec02b6e6c02d984 100644
--- a/dist/events-4c71f21f.cjs.prod.js
+++ b/dist/events-4c71f21f.cjs.prod.js
@@ -32,6 +32,50 @@ var React__namespace = /*#__PURE__*/_interopNamespace(React);
 var THREE__namespace = /*#__PURE__*/_interopNamespace(THREE);
 var Tb__default = /*#__PURE__*/_interopDefault(Tb);
 
+// Three.js deprecated Clock in r183. Keep Fiber's clock contract while
+// delegating time measurement to the supported Timer implementation.
+class TimerClock {
+  constructor(autoStart) {
+    if (autoStart === void 0) autoStart = true;
+    this.autoStart = autoStart;
+    this.startTime = 0;
+    this.oldTime = 0;
+    this.elapsedTime = 0;
+    this.running = false;
+    this.timer = new THREE__namespace.Timer();
+  }
+  start() {
+    this.timer.reset();
+    this.startTime = performance.now();
+    this.oldTime = this.startTime;
+    this.elapsedTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.getElapsedTime();
+    this.running = false;
+    this.autoStart = false;
+  }
+  getElapsedTime() {
+    this.getDelta();
+    return this.elapsedTime;
+  }
+  getDelta() {
+    var diff = 0;
+    if (this.autoStart && !this.running) {
+      this.start();
+      return 0;
+    }
+    if (this.running) {
+      this.timer.update();
+      diff = this.timer.getDelta();
+      this.oldTime = performance.now();
+      this.elapsedTime += diff;
+    }
+    return diff;
+  }
+}
+
 var threeTypes = /*#__PURE__*/Object.freeze({
   __proto__: null
 });
@@ -1038,7 +1082,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: new TimerClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-b1bdeb1a.cjs.dev.js b/dist/events-b1bdeb1a.cjs.dev.js
index 7785529f79299cdefe706c0df1d5c756817ca274..d99099d880ff7b1a411aca17d50309ecee908d8c 100644
--- a/dist/events-b1bdeb1a.cjs.dev.js
+++ b/dist/events-b1bdeb1a.cjs.dev.js
@@ -36,6 +36,50 @@ var threeTypes = /*#__PURE__*/Object.freeze({
   __proto__: null
 });
 
+// Three.js deprecated Clock in r183. Keep Fiber's clock contract while
+// delegating time measurement to the supported Timer implementation.
+class TimerClock {
+  constructor(autoStart) {
+    if (autoStart === void 0) autoStart = true;
+    this.autoStart = autoStart;
+    this.startTime = 0;
+    this.oldTime = 0;
+    this.elapsedTime = 0;
+    this.running = false;
+    this.timer = new THREE__namespace.Timer();
+  }
+  start() {
+    this.timer.reset();
+    this.startTime = performance.now();
+    this.oldTime = this.startTime;
+    this.elapsedTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.getElapsedTime();
+    this.running = false;
+    this.autoStart = false;
+  }
+  getElapsedTime() {
+    this.getDelta();
+    return this.elapsedTime;
+  }
+  getDelta() {
+    var diff = 0;
+    if (this.autoStart && !this.running) {
+      this.start();
+      return 0;
+    }
+    if (this.running) {
+      this.timer.update();
+      diff = this.timer.getDelta();
+      this.oldTime = performance.now();
+      this.elapsedTime += diff;
+    }
+    return diff;
+  }
+}
+
 /**
  * Returns the instance's initial (outmost) root.
  */
@@ -1038,7 +1082,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: new TimerClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
